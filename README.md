#Working Directory
- Area where all files and directories and changes are living all the time
- git init

#Staging Area
-Files and directories that we explicitly add to the staging area
- git add <filename>

#Git Repository
- Where all the snapshots/versions are . Each commit gives a new snapshot/version
-git commit <filename>